node_modules/
dist/
# 빌드 때 서버 config 에서 복사해 온다 (tools/copy-demo-config.mjs) — 원본이 단일 출처
packages/*/public/config/
*.log
.env
.env.*
!.env.example
# 배포 빌드용 프론트 설정 — 비밀값 없음(서버 주소가 '같은 도메인'이라는 표시뿐)
!.env.production
data/*.db
data/*.db-journal
packages/server/data/
mosquitto/data/
mosquitto/log/
