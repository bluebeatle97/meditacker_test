# 다음에 이어서 할 것

마지막 작업 2026-08-07 · 브랜치 `plan-v2-rebuild` (⚠️ `main` 아님)

---

## 바로 시작하는 법

```bash
npm run stop
npm run dev:all -- --no-mock     # 실장비 2대 구성
```

가상 인원을 보려면 `GATEWAYS_FILE=gateways.planned.json npm run dev:all`

화면: 직원용 `:5173` · 환자용 `:5174` · 관제 `:8080/monitor`

---

## 합의한 운영 방식

```
비콘 전원 항상 켜짐 → 신호 계속 옴
등록(평생 1회) → 창고 ⟷ 환자 등록(올 때마다) → 환자 손 → 반납 → 창고
배정된 것만 화면에 나옴. 미배정은 목록에서만 보임
```

- 인포가 **직원용 패널(5173)** 에서 환자 등록/반납
- 환자 이름은 **수기 입력**. 베가스 CRM 은 나중 (그때도 이 칸을 대신 채워줄 뿐)
- 직원 비콘은 한 번 등록하면 유지, 퇴사·입사 때만 재등록
- 실명 OK (내부 툴)
- 용어: **환자 등록** = 사람에게 비콘 넘기기 / **등록** = 관제에서 하드웨어 화이트리스트

---

## 단계

| | 내용 | 상태 |
|---|---|---|
| 1 | 등록/배정 분리 (배정마다 새 사람) | ✅ `bac0d4a` |
| 2 | 미배정 비콘 관문 제외 + 유휴 기록 | ✅ `85286ed` |
| 3+4+5 | 환자 등록/반납 API + 버튼 + 목록 | ✅ `5a62023` |
| 7 | QR 진입 + 첫 스캔 바인딩 | ✅ `27f6980` |
| **6** | **인포 데스크 게이트웨이 자동 반납** | ⬜ 다음 (반납함 게이트웨이 설치 후) |
| 8 | 베가스 CRM 연동 | 미정 |

---

## 6단계 — 자동 반납 (합의된 규칙)

인포 데스크 **보관함 안에** 게이트웨이를 따로 두고, 그 게이트웨이가
**30cm 이내(≈ −35dBm)로 5분 이상** 잡으면 자동 반납.

- ⚠️ 카운터 위가 아니라 **보관함 안**에 설치. 환자가 수납하느라 데스크에 5분 서 있으면
  차고 있는 팔찌가 반납돼 버린다. 물리적 분리가 임계값 튜닝보다 확실하다
- 임계값은 **실제 팔찌로 실측** 필요 (TX 파워·인체 감쇠로 달라짐)
- 게이트웨이 설정에 "반납함" 역할 표시가 하나 필요

참고 RSSI: 30cm ≈ −33 / 60cm ≈ −40 / 1m ≈ −45 / 2m ≈ −52

---

## 7단계 — QR 진입 (합의된 방식)

**"환자 등록 후 첫 스캔이 그 팔찌를 차지"**

```
인포: 환자 등록 → 팔찌 건네줌
환자: 그 자리에서 팔찌 QR 스캔 → 입력 없이 진입 → 그 기기가 이 배정의 주인이 됨
예전 환자: 나중에 브라우저 기록에서 링크 눌러도 "이미 사용 중" 거부
반납: 묶임 해제
```

- QR 에는 **핀(비콘 뒷 6자리)** 을 넣는다. 토큰은 만료되지만 인쇄물은 안 바뀐다
- 데스크에 **초기화 버튼** 필요 — 환자가 폰 바꾸거나 기록 지우면 못 들어옴
- 커스터마이징·카메라 추적은 **이미 다 구현돼 있음** — 토큰만 맞으면 그대로 동작
- QR 에 서버 주소가 박히므로 **고정 IP 정한 뒤** 인쇄할 것

---

## 미해결 (7단계 전에 정할 것)

- **QR 인쇄 시점** — 고정 IP(공유기 DHCP 예약) 먼저
- **직원용·관제 무인증** — 같은 망이고 실명이 뜬다. 포트만 바꾸면 `:5173`·`:8080` 이
  그대로 열린다. 공유 비밀번호 한 겹이면 막힌다 (로그인 시스템까지 갈 필요 없음)
- **게이트웨이 2대 실제 설치 좌표** — 지금 값은 도면에서 손으로 짚은 것

---

## 기타 상태

- 이력에서 DB blob 90MB 제거 + 강제 푸시 완료 → **기존 클론은 `git reset --hard` 필요**
- 백업: `C:\Users\LG gram\meditracker-backup-20260806-1831.bundle`
- 앉기 자세는 넣었으나 **화면 확인 미완** (원본 스프라이트에 의자가 없어 어색할 수 있음)
- 테스트 82개 통과
